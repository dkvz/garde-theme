// This is ES5 because browser JS could require this file.
var conf = {
  languages: ['fr', 'en'],
  // The main index page will be generated using the
  // default language.
  defaultLanguage: 'fr',
  // This is passed to the templates and used in some metatags:
  // (NO TRAILING SLASH Plz Sorry For CaPS)
  absoluteUrl: 'https://2018.lamusiquedelagarde.be'
};

exports.conf = conf;